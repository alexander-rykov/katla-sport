export class HiveListItem {
    constructor(
        public id: number,
        // TODO STEP 6 - Add parameters name and code here.
        public name: string,
        public code: string,
        public isDeleted: boolean
    ) { }
}
